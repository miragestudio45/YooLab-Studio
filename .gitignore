# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*

# deployment and generated files
.vercel
next-env.d.ts
/dist/
# Nitro build output. `.vercel/output` (the vercel preset) is already covered by
# `.vercel` above; this is where every other Nitro preset writes.
/.output/
/.nitro/
/.wrangler/
/outputs/
/work/

# reference-only research clones — never shipped
/reference-sources/

reference-sources.zip

# Downloaded third-party sources, re-processed into public/asset by scripts/.
# Not committed: they are inputs, and THIRD_PARTY_ASSETS.md records where they
# come from and how to fetch them again.
.cache/
